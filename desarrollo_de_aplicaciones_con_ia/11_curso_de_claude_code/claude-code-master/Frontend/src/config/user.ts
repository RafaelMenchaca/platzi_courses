/**
 * Identidad de usuario (mock)
 *
 * El Frontend todavía no tiene autenticación, pero la API de ratings exige un
 * `user_id`. Usamos un id fijo para desbloquear el flujo de calificación.
 *
 * TODO: sustituir por el id real del usuario autenticado cuando exista auth.
 */
export const MOCK_USER_ID = 42;
